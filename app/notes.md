
### General
- `${name}.ts` - Main

### Additional

These have to be mentioned in Main.

- `${name}.html` - custom html
- `${name}.css` - custom styles
- `${name}.component.ts` - the component we want to render
- `${name}.spec.ts` - Unit testing